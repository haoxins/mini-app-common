{
  "name": "mini-app-common",
  "version": "1.0.1",
  "main": "dist/local-storage.js",
  "types": "dist/local-storage.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "repository": "haoxins/mini-app-common",
  "author": "haoxin",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  }
}
